L'istruzione __break__ può essere usata per interrompere l'esecuzione di un costrutto [[Iteratore|iterativo]].
```python
while True:
	s = input("Inserisci una stringa (o solo <enter> per terminare):")
	if not s:
		break
	print(s)
```

