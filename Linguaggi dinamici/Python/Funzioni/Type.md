__Type__ è sia una [[funzione]] che una [[Classe]], è utile per conoscere il tipo di un oggetto.
Type è anche una classe, molto particolare, che è _tipo di se stessa_.
Infatti si può scrivere:
```jupyter
type(type)
```
